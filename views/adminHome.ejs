<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin - SpaceWise</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
    }
  </style>
</head>
<body class="bg-gray-100">
  <!-- Pop-up notification for login success message -->
  <% if (successMessage) { %>
    <div id="popup-message" class="fixed top-5 left-1/2 transform -translate-x-1/2 bg-green-500 text-white py-2 px-4 rounded-lg shadow-lg opacity-0 transition-opacity duration-300 ease-in-out">
        <%= successMessage %>
    </div>
    <script>
        window.onload = function () {
            const popup = document.getElementById('popup-message');
            if (popup) {
                popup.classList.remove('opacity-0'); // Show the popup
                setTimeout(() => { popup.classList.add('opacity-0'); }, 3000); // Auto-hide after 3s
                popup.addEventListener('click', () => popup.classList.add('opacity-0')); // Hide on click
            }
        };
    </script>
  <% } %>

  <!-- Admin Navbar -->
  <%- include('partials/adminNavbar', { institutionId: institutionId }) %>

  <!-- Admin Content -->
  <main class="container mx-auto py-10 px-6">
    <h3 class="text-xl font-semibold mb-2">Admin Dashboard</h3>
    <p class="text-gray-600 text-sm mb-6">Welcome to the admin dashboard. Use the options below to manage your institution and the spaces and reservations within it.</p>
    
    <!-- Section for managing the institution -->
    <div class="bg-white p-6 mb-6 shadow-md rounded-lg flex items-center">
      <div class="ml-4 flex-1">
        <h4 class="text-lg font-semibold">Manage Your Institution</h4>
        <p class="text-gray-600 text-sm">Edit the details of your institution or close your account.</p>
      </div>
      <div class="ml-4">
        <a href="/institutions/<%= institutionId %>">
          <button class="px-4 py-2 text-sm text-white bg-purple-600 rounded hover:bg-purple-800">
            Manage
          </button>
        </a>
      </div>
    </div>

    <!-- Section for managing spaces -->
    <div class="bg-white p-6 mb-6 shadow-md rounded-lg flex items-center">
      <div class="ml-4 flex-1">
        <h4 class="text-lg font-semibold">Manage Spaces</h4>
        <p class="text-gray-600 text-sm">Edit the spaces within your institution or create a new one.</p>
      </div>
      <div class="ml-4">
        <a href="/institutions/<%= institutionId %>/spaces">
          <button class="px-4 py-2 text-sm text-white bg-purple-600 rounded hover:bg-purple-800">
            Manage
          </button>
        </a>
      </div>
    </div>

    <!-- Section for managing reservations -->
    <div class="bg-white p-6 mb-6 shadow-md rounded-lg flex items-center">
      <div class="ml-4 flex-1">
        <h4 class="text-lg font-semibold">Manage Reservations</h4>
        <p class="text-gray-600 text-sm">See all the reservations made in your institution with an option to override them.</p>
      </div>
      <div class="ml-4">
        <a href="/reservations">
            <button class="px-4 py-2 text-sm text-white bg-purple-600 rounded hover:bg-purple-800">
              Manage
            </button>
        </a>
      </div>
    </div>
  </main>
</body>
</html>
