<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Space Selection - SpaceWise</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <link rel="icon" type="image/png" href="/favicon.png">
  <style>
    body {
      font-family: 'Inter', sans-serif;
    }
  </style>
</head>
<body class="bg-gray-100">
  <!-- Student Navbar -->
  <%- include('partials/studentNavbar') %>

  <!-- Main Content -->
  <main class="container mx-auto py-10 px-6">
    <h2 class="text-2xl font-semibold mb-6">Select a Space in <%= institutionName %></h2>
    <ul class="space-y-4">
      <% if (spaces.length > 0) { %>
        <% spaces.forEach(space => { %>
          <li class="bg-white shadow-md rounded-lg p-4 flex justify-between items-center">
            <div>
              <h2 class="text-lg font-bold"><%= space.space_name %></h2>
            </div>
            <a href="/institutions/<%= space.institution_id %>/spaces/<%= space.space_id %>">
              <button class="px-4 py-2 text-sm text-white bg-purple-600 rounded hover:bg-purple-800">
                Select
              </button>
            </a>
          </li>
        <% }) %>
      <% } else { %>
        <p class="text-gray-600">No spaces available.</p>
      <% } %>
    </ul>
  </main>
</body>
</html>
