<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Space Blueprint - SpaceWise</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
    }
    .seat-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(50px, 1fr));
      gap: 10px;
    }
    .seat {
      display: flex;
      align-items: center;
      justify-content: center;
      height: 50px;
      border: 2px solid #ccc;
      border-radius: 4px;
      background-color: #f9fafb;
    }
    .seat.unavailable {
      background-color: #f87171;
      color: white;
    }
    .seat.available {
      background-color: #34d399;
      color: white;
    }
  </style>
</head>
<body class="bg-gray-100">
  <!-- Navbar -->
  <header class="bg-white shadow-md">
    <div class="container mx-auto flex items-center justify-between py-4 px-6">
      <div class="flex items-center space-x-2">
        <a href="/home" class="text-3xl font-bold">𐌂</a> <!-- Clickable Logo -->
        <span class="text-xl font-semibold">SpaceWise</span>
      </div>
    </div>
  </header>

  <!-- Main Content -->
  <main class="container mx-auto py-10 px-6">
    <h2 class="text-2xl font-semibold mb-6">Manage Space Details</h2>

    <div class="bg-white p-6 shadow-md rounded-lg">
      <!-- Upload/Edit Layout -->
      <h3 class="text-xl font-semibold mb-4">Upload/Edit Layout</h3>
      <form class="mb-6" action="/institutions/4/spaces" method="POST">
        <label for="layoutUpload" class="block text-sm font-medium text-gray-700">Upload Layout Image</label>
        <input type="file" id="layoutUpload" class="mt-2 block w-full text-sm text-gray-700 bg-gray-50 border border-gray-300 rounded-md focus:outline-none focus:ring-purple-600 focus:border-purple-600">
        <button type="submit" class="mt-4 px-4 py-2 text-sm text-white bg-purple-600 rounded hover:bg-purple-800">
          Save Layout
        </button>
      </form>

      <!-- Seat Management -->
      <h3 class="text-xl font-semibold mb-4">Seat Management</h3>
      <div class="mb-6">
        <button class="px-4 py-2 text-sm bg-green-600 text-white rounded hover:bg-green-800">Add Seat</button>
        <button class="ml-4 px-4 py-2 text-sm bg-blue-600 text-white rounded hover:bg-blue-800">Move Seat</button>
        <button class="ml-4 px-4 py-2 text-sm bg-red-600 text-white rounded hover:bg-red-800">Delete Seat</button>
      </div>

      <!-- Facilities Management -->
      <h3 class="text-xl font-semibold mb-4">Update Facilities</h3>
      <form class="mb-6" action="insititutions/3/spaces/:spaceId" method="POST">
        <label for="facilityUpdate" class="block text-sm font-medium text-gray-700">Add/Remove Seat Facilities</label>
        <input type="text" id="facilityUpdate" placeholder="e.g., Outlet, Table" class="mt-2 block w-full px-3 py-2 bg-gray-50 border border-gray-300 rounded-md focus:outline-none focus:ring-purple-600 focus:border-purple-600">
        <button type="submit" class="mt-4 px-4 py-2 text-sm text-white bg-purple-600 rounded hover:bg-purple-800">
          Update Facilities
        </button>
      </form>

      <!-- Seat Availability -->
      <h3 class="text-xl font-semibold mb-4">Seat Availability</h3>
      <div class="mb-6">
        <label for="seatSearch" class="block text-sm font-medium text-gray-700">Search Seats</label>
        <input type="text" id="seatSearch" placeholder="Search by seat number or facility" class="mt-2 block w-full px-3 py-2 bg-gray-50 border border-gray-300 rounded-md focus:outline-none focus:ring-purple-600 focus:border-purple-600">
        
        <!-- <div class="mt-4 seat-grid">
          <div class="seat available">1</div>
          <div class="seat unavailable">2</div>
          <div class="seat available">3</div>
          <div class="seat unavailable">4</div>
        </div> -->
      </div>

      <!-- Space Preview -->
      <h3 class="text-xl font-semibold mb-4">Preview Space</h3>
      <div class="p-4 bg-gray-50 border rounded-md">
        <p class="text-gray-600 text-sm">Preview the space layout as seen by students.</p>
        <div class="mt-4 bg-gray-200 h-64 flex items-center justify-center">
          <span class="text-gray-500">Space layout preview here...</span>
        </div>
      </div>
    </div>
  </main>
</body>
</html>
