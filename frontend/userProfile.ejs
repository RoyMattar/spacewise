<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Profile - SpaceWise</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
    }
  </style>
</head>
<body class="bg-gray-100">
  <!-- Navbar -->
  <%- include('header') %>

  <!-- Main Content -->
  <main class="container mx-auto py-10 px-6">
    <h2 class="text-2xl font-semibold mb-6">Welcome Back, <%= username %>! (User Profile)</h2>

    <div class="bg-white p-6 shadow-md rounded-lg">
      <form action="/updateProfile" method="POST" enctype="multipart/form-data">
        <!-- Username -->
        <div class="mb-4">
          <label for="username" class="block text-sm font-medium text-gray-700">Change Username</label>
          <input type="text" id="username" name="username" placeholder="New Username" class="mt-1 block w-full px-3 py-2 bg-gray-50 border border-gray-300 rounded-md focus:outline-none focus:ring-purple-600 focus:border-purple-600">
        </div>

        <!-- Password -->
        <div class="mb-4">
          <label for="password" class="block text-sm font-medium text-gray-700">Change Password</label>
          <input type="password" id="password" name="password" placeholder="New Password" class="mt-1 block w-full px-3 py-2 bg-gray-50 border border-gray-300 rounded-md focus:outline-none focus:ring-purple-600 focus:border-purple-600">
        </div>

        <!-- Profile Picture -->
        <div class="mb-4">
          <label for="profilePicture" class="block text-sm font-medium text-gray-700">Select Profile Picture or Emoji</label>
          <input type="file" id="profilePicture" name="profilePicture" accept="image/*" class="mt-1 block w-full text-sm text-gray-700 bg-gray-50 border border-gray-300 rounded-md cursor-pointer focus:outline-none">
          <p class="mt-2 text-sm text-gray-500">Or select an emoji below:</p>
          <div class="flex space-x-4 mt-2">
            <button type="button" class="text-2xl">😀</button>
            <button type="button" class="text-2xl">🚀</button>
            <button type="button" class="text-2xl">🌟</button>
            <button type="button" class="text-2xl">🏆</button>
          </div>
        </div>

        <button type="submit" class="w-full px-4 py-2 text-sm text-white bg-purple-600 rounded hover:bg-purple-800">
          Save Changes
        </button>
      </form>
      <a href="/">
        <button class="w-full px-4 py-2 text-sm text-white bg-black rounded hover:bg-gray-800 mt-4">
          Return Home
        </button>
      </a>
    </div>
  </main>

  <!-- Footer -->
  <%- include('footer') %>
</body>
</html>
